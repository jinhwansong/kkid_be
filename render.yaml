# Render Blueprint — 대시보드만 쓸 때: Build npm ci && npm run build / Start npm start
# Node: .node-version | Nest 산출물은 dist/main.js (tsconfig.build.json rootDir: src)
services:
  - type: web
    name: kkid-be
    runtime: node
    buildCommand: npm ci && npm run build
    startCommand: npm start
